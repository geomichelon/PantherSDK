Put your demo video here as `demo.mov` (or `demo.mp4`).

GitHub will render it inline in the root `README.md` using this relative path:

- `docs/media/demo.mov`

Tip: keep it under ~20–50 MB for faster loading in the web UI.
