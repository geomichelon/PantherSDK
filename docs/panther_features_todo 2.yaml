---
# Pending roadmap work derived from panther_features.yaml
metadata:
  source: panther_features.yaml
  version: "1.0"
  generated_by: codex_cli
  generated_at: 2024-03-17

features:
  - key: core_runtime
    status: active
    pending_tasks:
      - deliver async implementation for LlmProvider trait
      - ship production adapters for OpenAI, Ollama, and Anthropic providers
      - introduce tokenizer abstraction for prompt and output handling

  - key: storage_and_data
    status: in_progress
    pending_tasks:
      - define persistent schema for evaluations and metrics
      - implement SQL analytics layer (SQLite and Postgres targets)
      - support export and import of .pantherdb snapshots

  - key: ffi_cross_platform
    status: in_progress
    pending_tasks:
      - automate binding generation (e.g. UniFFI, flutter_rust_bridge)
      - create cross-FFI integration test suite covering Swift, Kotlin, Flutter, and RN

  - key: python_api
    status: in_progress
    pending_tasks:
      - add policy evaluation workflows to API surface
      - implement JWT authentication and audit logging
      - integrate guidelines validation endpoints

  - key: compliance_guidelines
    status: planned
    pending_tasks:
      - integrate guideline ingestion from cloud drives or S3 buckets
      - add vector search with similarity scoring for guideline matching
      - compute trust_index and bias_score for evaluation outputs

  - key: blockchain_verification
    status: planned
    pending_tasks:
      - build verification pipeline inside Rust core
      - publish proof of integrity to blockchain registry
      - validate proofs during compliance checks

  - key: observability_metrics
    status: in_progress
    pending_tasks:
      - expose Prometheus exporter for runtime metrics
      - aggregate latency metrics (p50, p95)
      - correlate tracing spans across FFI boundaries

  - key: developer_experience
    status: in_progress
    pending_tasks:
      - deliver panther-cli developer tool
      - expand automation around sample projects
      - unify packaging for mobile and desktop SDK bundles

  - key: samples
    status: in_progress
    pending_tasks:
      - add compliance report generation to demos
      - include bias and trust test flows across all samples

  - key: future_extensions
    status: planned
    pending_tasks:
      - panther_cloud_shared_guidelines
      - explainability_layer
      - llm_sandbox
      - dataset_marketplace
      - edge_webgpu_execution
